<%= render "form.html", Map.merge(assigns, %{
  action: site_post_path(@conn, :update, @site.name, @post.slug),
  title: "Editar artículo"
})%>

<span><%= link "Regresar", to: site_post_path(@conn, :index, @site.name) %></span>
