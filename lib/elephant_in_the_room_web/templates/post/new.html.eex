<%= render "form.html", Map.merge(assigns, %{
  action: site_post_path(@conn, :create, @site),
  title: "Nuevo Artículo"
})%>

<span><%= link "Regresar", to: site_post_path(@conn, :index, @site) %></span>
